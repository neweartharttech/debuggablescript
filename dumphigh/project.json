{
  "name": "dumphigh",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "dumphigh/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "dumphigh:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "dumphigh:build:development"
        },
        "production": {
          "buildTarget": "dumphigh:build:production"
        }
      }
    }
  }
}
